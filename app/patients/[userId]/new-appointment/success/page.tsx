import React from 'react'

const RequestSuccess = async ({
    searchParams,
    params: { userId }
}: SearchParamProps) => {
  return (
    <div className='flex h-screen max-h-screen px-[5%]'>RequestSuccess</div>
  )
}

export default RequestSuccess